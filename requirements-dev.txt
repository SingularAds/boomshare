# Local development and CI. Installs the production set plus test / lint tools.
# Production images install requirements.txt only.
-r requirements.txt

# Test suite
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0      # async SQLite driver: tests + the local sandbox only
fakeredis==2.37.1
respx==0.23.1

# Lint / format
ruff==0.8.6
